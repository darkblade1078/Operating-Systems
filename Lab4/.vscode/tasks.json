{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake FunkyBuild",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build" // Adjust the build directory as necessary
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
