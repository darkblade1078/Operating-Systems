{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Lab9",
            "preLaunchTask": "CMake: build",
            "cwd": "${workspaceFolder}/build/"
        }
    ]
}
