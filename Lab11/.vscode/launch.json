{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Lab11",
            "preLaunchTask": "CMake: build",
            "cwd": "${workspaceFolder}/build/"
        }
    ]
}
